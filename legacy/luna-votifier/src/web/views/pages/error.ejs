<%- include('../partials/top', { currentGuild: null }) %>

<section class="verify">
  <div class="login-card danger">
    <p class="eyebrow">오류</p>
    <h1><%= title %></h1>
    <p><%= message %></p>
    <a class="btn btn-ghost" href="/">홈으로 이동</a>
  </div>
</section>

<%- include('../partials/bottom') %>
