# The CNAME target is routing infrastructure, not a public MineWiki site.
# Customer hostnames receive exact generated vhosts from the domain provisioner.
server {
  listen 80;
  listen [::]:80;
  server_name domains.minewiki.kr;

  location / {
    return 404;
  }
}
